**Fixed:**

* Replace renamed and outdated ``.wdf`` test fixtures with current sample files.
* Extend test coverage for previously untested ``WDFReader`` properties and lazy loading.
* Fix a hardcoded fixture count in the catalog test suite.
